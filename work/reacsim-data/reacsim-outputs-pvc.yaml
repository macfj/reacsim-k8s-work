apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reacsim-outputs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: reacsim-outputs-pv
  storageClassName: azureblob-fuse-premium