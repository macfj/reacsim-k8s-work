apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reacsim-inputs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: reacsim-inputs-pv
  storageClassName: azureblob-fuse-premium